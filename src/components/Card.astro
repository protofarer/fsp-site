---
interface Props {
  class?: string;
  variant?: 'dark' | 'light';
  id?: string;
}

const { class: className = '', variant = 'light', id } = Astro.props;

const classCard = 'p-4 md:p-6 rounded-lg space-y-4 shadow-md md:shadow-lg';
let classTypography = ''
let colorBackground = '';
let classBorder = '';

if (variant === 'dark') {
  colorBackground = 'bg-gray-800';
  classBorder = 'border-2 border-solid border-black';
  // TODO: needs to be overridable by the slot classes
  // classTypography ='text-white'
  // classTypography = '[&_*]:text-white'
} else if (variant === 'light') {
  colorBackground = 'bg-white';
  classBorder = 'md:border md:border-solid md:border-gray-200';
  // classTypography ='text-black'
  // classTypography = '[&_*]:text-black'
}

const computedClass = [
  classCard,
  colorBackground,
  classBorder,
  classTypography,
  className, // leave this last so it overrides above classes
];
---

<div id={id} class:list={computedClass}>
  <slot />
</div>
